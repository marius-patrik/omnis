name: Auto Format

on:
  push:
    branches: ["**"]

# The job bodies and the scripts they run come from the pinned pipeline. This file is a caller,
# not a copy.
permissions:
  contents: write

jobs:
  run:
    uses: marius-patrik/DarkFactory/.github/workflows/auto-format.yml@8f2418f5ad6e12ec179ddd2fa20e3520edd8db42
    with:
      pipeline-repo: marius-patrik/DarkFactory
      pipeline-ref: 8f2418f5ad6e12ec179ddd2fa20e3520edd8db42
